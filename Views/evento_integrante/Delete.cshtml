@model Worship.evento_integrante

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>evento_integrante</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.evento.tx_comentarios)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.evento.tx_comentarios)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.integrante.tx_nome_integrante)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.integrante.tx_nome_integrante)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
